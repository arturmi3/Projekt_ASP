Przed uruchomieniem należy koniecznie zrobić update-database.\
Po uruchomieniu otrzymamy widok zwykłego użytkownika, widok administratora otrzymamy po zarejestrowaniu się i wprowadzeniu danych z rejestracji w oknie logowania.
